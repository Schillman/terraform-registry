---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/docker/container/main.tf
  - modules/docker/container/variables.tf
  - modules/docker/container/outputs.tf
  - modules/docker/container/versions.tf
  - modules/docker/container/README.md
  - modules/docker/container/tests/example/main.tf
  - modules/docker/container/tests/example/.terraform.lock.hcl
autonomous: true
requirements:
  - STRC-01
  - STRC-02
  - STRC-03
  - STRC-04
  - STRC-05
  - STRC-06

must_haves:
  truths:
    - "modules/docker/container/ exists and contains exactly main.tf, variables.tf, outputs.tf, versions.tf, README.md, and tests/"
    - "versions.tf declares required_version = \"~> 1.9\" alongside the kreuzwerker/docker 3.0.2 provider block"
    - "No file named terraform.tf exists anywhere in modules/docker/container/"
    - "outputs.tf exposes container_id (docker_container.main.id), container_name (docker_container.main.name), image_id (docker_image.main.image_id)"
    - "modules/terraform-docker-container/ directory does not exist; old git tags v0.0.1 and v1.0 are preserved"
    - "tests/example/main.tf uses source = \"../../\" (relative path, not the old GitHub source URL)"
  artifacts:
    - path: "modules/docker/container/main.tf"
      provides: "Docker image, volume, and container resource definitions"
      contains: "docker_image.main, docker_volume.volumes, docker_container.main"
    - path: "modules/docker/container/variables.tf"
      provides: "All input variable declarations for the module"
    - path: "modules/docker/container/outputs.tf"
      provides: "Three output values: container_id, container_name, image_id"
      exports: ["container_id", "container_name", "image_id"]
    - path: "modules/docker/container/versions.tf"
      provides: "terraform {} block with required_version and provider requirements"
      contains: "required_version = \"~> 1.9\""
    - path: "modules/docker/container/README.md"
      provides: "Module documentation"
    - path: "modules/docker/container/tests/example/main.tf"
      provides: "Working local example using relative source path"
      contains: "source = \"../../\""
    - path: "modules/docker/container/tests/example/.terraform.lock.hcl"
      provides: "Lockfile for reproducible example init"
  key_links:
    - from: "modules/docker/container/outputs.tf"
      to: "modules/docker/container/main.tf"
      via: "resource attribute references"
      pattern: "docker_container\\.main\\.(id|name)|docker_image\\.main\\.image_id"
    - from: "modules/docker/container/versions.tf"
      to: "terraform runtime"
      via: "required_version constraint"
      pattern: "required_version.*~> 1\\.9"
---

<objective>
Migrate the Docker container module from its flat legacy path to the permanent namespaced path required by all downstream tooling.

Purpose: terraform-module-releaser, terraform-docs, and the tag format `modules/docker/container/vX.Y.Z` all require modules to live at `modules/{provider}/{resource}`. This migration is the unconditional prerequisite for every other phase.

Output: `modules/docker/container/` with all six required files (main.tf, variables.tf, outputs.tf, versions.tf, README.md, tests/), old directory deleted, old git tags preserved.
</objective>

<execution_context>
@/Users/p950cvo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/p950cvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing source files to migrate. Executor reads these — no exploration needed. -->

From modules/terraform-docker-container/terraform.tf (becomes versions.tf body):
```hcl
terraform {
  required_providers {
    docker = {
      source  = "kreuzwerker/docker"
      version = "3.0.2"
    }
  }
}
```
Add `required_version = "~> 1.9"` inside the `terraform {}` block — this is the ONLY change to the provider block content.

Target outputs.tf (complete file, user-locked):
```hcl
output "container_id" {
  description = "The ID of the Docker container."
  value       = docker_container.main.id
}

output "container_name" {
  description = "The name of the Docker container."
  value       = docker_container.main.name
}

output "image_id" {
  description = "The ID of the Docker image used by the container."
  value       = docker_image.main.image_id
}
```
NOTE: `image_id` uses `docker_image.main.image_id` (NOT `.id`). These are different attributes on the kreuzwerker/docker provider. The user explicitly specified `image_id`.

Target tests/example/main.tf source path: `source = "../../"` (relative path — the old GitHub URL points to a deleted directory and must not be preserved verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create modules/docker/container/ with all six required files</name>
  <files>modules/docker/container/main.tf, modules/docker/container/variables.tf, modules/docker/container/outputs.tf, modules/docker/container/versions.tf, modules/docker/container/README.md, modules/docker/container/tests/example/main.tf, modules/docker/container/tests/example/.terraform.lock.hcl</files>
  <action>
    1. Create the target directory structure:
       ```
       mkdir -p modules/docker/container/tests/example
       ```

    2. Copy main.tf and variables.tf verbatim (do not alter content — terraform fmt must still pass):
       ```
       cp modules/terraform-docker-container/main.tf modules/docker/container/main.tf
       cp modules/terraform-docker-container/variables.tf modules/docker/container/variables.tf
       cp modules/terraform-docker-container/README.md modules/docker/container/README.md
       cp modules/terraform-docker-container/tests/example/.terraform.lock.hcl modules/docker/container/tests/example/.terraform.lock.hcl
       ```

    3. Create versions.tf — do NOT copy terraform.tf verbatim. Write this exact file content:
       ```hcl
       terraform {
         required_version = "~> 1.9"

         required_providers {
           docker = {
             source  = "kreuzwerker/docker"
             version = "3.0.2"
           }
         }
       }
       ```
       The `required_version` line is new; the provider block is migrated as-is from terraform.tf.

    4. Create outputs.tf — write exactly:
       ```hcl
       output "container_id" {
         description = "The ID of the Docker container."
         value       = docker_container.main.id
       }

       output "container_name" {
         description = "The name of the Docker container."
         value       = docker_container.main.name
       }

       output "image_id" {
         description = "The ID of the Docker image used by the container."
         value       = docker_image.main.image_id
       }
       ```
       CRITICAL: Use `docker_image.main.image_id` (not `.id`) for the image_id output. This is the user-locked decision.

    5. Create tests/example/main.tf — read the existing file at `modules/terraform-docker-container/tests/example/main.tf` and copy it, BUT replace the `source` line with `source = "../../"`. The old source URL (`github.com/Schillman/terraform-registry//modules/terraform-docker-container?ref=v1.0`) points to the deleted directory — it must be updated to the relative path so local terraform init works.

    6. Run terraform fmt to confirm all .tf files pass format check:
       ```
       terraform fmt -check -recursive modules/docker/container/
       ```
       If any file fails, run `terraform fmt -recursive modules/docker/container/` to auto-fix, then review the diff.
  </action>
  <verify>
    <automated>ls modules/docker/container/ && grep "required_version" modules/docker/container/versions.tf && grep "image_id.*docker_image.main.image_id" modules/docker/container/outputs.tf && grep 'source.*../../' modules/docker/container/tests/example/main.tf && terraform fmt -check -recursive modules/docker/container/</automated>
  </verify>
  <done>
    - modules/docker/container/ contains: main.tf, variables.tf, outputs.tf, versions.tf, README.md, tests/
    - versions.tf contains `required_version = "~> 1.9"` and the kreuzwerker/docker 3.0.2 provider block
    - No file named terraform.tf exists in modules/docker/container/
    - outputs.tf exposes container_id, container_name, image_id with correct attribute references
    - tests/example/main.tf uses `source = "../../"` (relative path)
    - All .tf files pass `terraform fmt -check`
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete old module directory</name>
  <files>modules/terraform-docker-container/ (deleted)</files>
  <action>
    Remove the old module directory entirely. The user decision (CONTEXT.md) is full deletion — no deprecation stub.

    ```
    rm -rf modules/terraform-docker-container/
    ```

    Verify the new path is intact before deletion (should already be confirmed by Task 1 verify step). Old git tags (v0.0.1, v1.0) are preserved automatically — deleting the directory does NOT delete git tags. Do not run `git tag -d` or any tag deletion command.

    Confirm the old directory is gone:
    ```
    ls modules/terraform-docker-container/ 2>&1 | grep "No such file"
    ```

    Confirm the new directory still exists:
    ```
    ls modules/docker/container/
    ```

    Stage both the new directory and the deletion for commit:
    ```
    git add modules/docker/container/
    git add -u modules/terraform-docker-container/
    ```

    Commit with a conventional commit message:
    ```
    git commit -m "feat: migrate docker container module to namespaced path modules/docker/container"
    ```
    This message uses `feat:` because it establishes the namespaced module structure required by terraform-module-releaser (a new capability, not just a refactor).
  </action>
  <verify>
    <automated>test ! -d modules/terraform-docker-container && ls modules/docker/container/main.tf modules/docker/container/outputs.tf modules/docker/container/versions.tf && echo "Migration complete"</automated>
  </verify>
  <done>
    - modules/terraform-docker-container/ directory does not exist (git ls-files shows no files at that path)
    - modules/docker/container/ exists with all six required files
    - Git tags v0.0.1 and v1.0 still exist (run `git tag` to confirm — tags are unaffected by directory deletion)
    - One commit recorded in git log for the migration
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls modules/docker/container/` returns: main.tf, outputs.tf, README.md, tests/, variables.tf, versions.tf
2. `ls modules/docker/container/tests/example/` returns: .terraform.lock.hcl, main.tf
3. `grep "required_version" modules/docker/container/versions.tf` returns a line containing `~> 1.9`
4. `ls modules/docker/container/terraform.tf 2>&1` returns "No such file or directory" (file must not exist)
5. `grep "image_id.*docker_image.main.image_id" modules/docker/container/outputs.tf` returns a match
6. `grep 'source.*../../' modules/docker/container/tests/example/main.tf` returns a match
7. `test ! -d modules/terraform-docker-container && echo "old dir gone"` outputs "old dir gone"
8. `git tag | grep -E "v0\.0\.1|v1\.0"` confirms old tags still exist
9. `terraform fmt -check -recursive modules/docker/container/` exits 0 (no formatting issues)
</verification>

<success_criteria>
- modules/docker/container/ exists with exactly six required files (STRC-01, STRC-02)
- versions.tf (not terraform.tf) is the file containing the terraform {} block with required_version = "~> 1.9" (STRC-03, STRC-04)
- All module content migrated from modules/terraform-docker-container/ (STRC-05)
- Old directory deleted, old git tags preserved (STRC-06 — user-modified: stub waived, tags preserved)
- terraform fmt -check passes on all .tf files in the new path
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
