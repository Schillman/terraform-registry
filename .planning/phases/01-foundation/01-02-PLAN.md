---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - CLAUDE.md
  - .markdownlintignore
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-05
  - MAINT-02

must_haves:
  truths:
    - "SKILL.md at repo root contains all four required sections: commit convention table, module scaffold pattern, autonomy matrix, consumer source URL pattern"
    - "SKILL.md commit convention table shows docs:/chore:/refactor:/test:/ci: as patch bump (not 'no release') — matching the locked user decision"
    - "SKILL.md consumer source URL example uses ref=modules/docker/container/v1.0.0 format and depth=1 does NOT appear in any version-pinned example"
    - "CLAUDE.md at repo root is a short file that instructs Claude Code agents to read SKILL.md — it does NOT duplicate SKILL.md content inline"
    - ".markdownlintignore at repo root contains .planning/ to exclude internal planning docs from markdownlint"
  artifacts:
    - path: "SKILL.md"
      provides: "Agent operating conventions for this repo"
      contains: "commit convention table, module scaffold, autonomy matrix, consumer URL pattern"
      min_lines: 40
    - path: "CLAUDE.md"
      provides: "Claude Code agent bootstrap pointer to SKILL.md"
      contains: "SKILL.md"
    - path: ".markdownlintignore"
      provides: "Excludes .planning/ from markdownlint"
      contains: ".planning/"
  key_links:
    - from: "CLAUDE.md"
      to: "SKILL.md"
      via: "explicit reference/mention"
      pattern: "SKILL\\.md"
    - from: "SKILL.md consumer URL example"
      to: "no depth=1 in version-pinned refs"
      via: "absence check"
      pattern: "depth=1"
---

<objective>
Write the agent operating conventions that every Claude Code session in this repo reads on startup, and exclude internal planning docs from markdown linting.

Purpose: Before any agent commits to this repo, it must know the correct commit types, what it can do autonomously, how to scaffold new modules, and how to reference modules as a consumer. SKILL.md is the single source of truth. CLAUDE.md is the Claude Code bootstrap pointer.

Output: SKILL.md (agent operating manual), CLAUDE.md (thin Claude Code pointer), .markdownlintignore (markdownlint exclusion).
</objective>

<execution_context>
@/Users/p950cvo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/p950cvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- All content is dictated by locked user decisions in CONTEXT.md. Executor must follow exactly. -->

SKILL.md required sections (locked):
1. Commit Convention Table — maps commit type to semver impact
2. Module Scaffold Pattern — required files per module
3. Autonomy Matrix — what agents can/cannot do without asking
4. Consumer Source URL Pattern — correct ref format, depth=1 explicitly forbidden

Commit convention table (LOCKED — user decision overrides REQUIREMENTS.md):
| Commit Type | Semver Impact |
|-------------|---------------|
| feat: | minor bump (1.x.0) |
| fix: | patch bump (1.0.x) |
| docs:, chore:, refactor:, test:, ci: | patch bump |
| BREAKING CHANGE: footer or feat!:/fix!: | major bump (x.0.0) |

NOTE: The above table uses "patch bump" for docs:/chore:/etc. REQUIREMENTS.md says "no release" for these types — but CONTEXT.md locks "patch bump". CONTEXT.md wins. Write "patch bump" in SKILL.md.

Autonomy matrix (LOCKED):
| Operation | Agent Autonomy |
|-----------|----------------|
| Edit/create .tf, .md, .yml, .json files | Freely — no approval needed |
| Edit CI workflows (lint.yaml, release.yaml, etc.) | Freely — no approval needed |
| Delete files | Must ask human first |
| Force push | Must ask human first |
| Create releases | Must ask human first |
| Modify branch protection rules | Must ask human first |

Consumer source URL pattern (LOCKED):
```hcl
# CORRECT — version-pinned ref, no depth=1
module "container" {
  source = "github.com/Schillman/terraform-registry//modules/docker/container?ref=modules/docker/container/v1.0.0"
}

# WRONG — depth=1 breaks once newer commits land after the tag
# Do NOT use: ?depth=1&ref=modules/docker/container/v1.0.0
```

CLAUDE.md approach (LOCKED): Short file, @-references or mentions SKILL.md by name, no duplication of SKILL.md content inline.

.markdownlintignore content (LOCKED): Single line `.planning/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md — agent operating manual</name>
  <files>SKILL.md</files>
  <action>
    Create SKILL.md at the repo root. The file must contain exactly these four sections in this order. Exact wording and formatting are at Claude's discretion within the locked constraints below.

    **Section 1: Commit Conventions**
    A table mapping commit type to semver impact. Use the locked user decision (not REQUIREMENTS.md):
    - `feat:` → minor bump
    - `fix:` → patch bump
    - `docs:`, `chore:`, `refactor:`, `test:`, `ci:` → patch bump
    - `BREAKING CHANGE:` footer OR `feat!:`/`fix!:` prefix → major bump

    Include a note that `feat!:`/`fix!:` shorthand is preferred over `BREAKING CHANGE:` footer because squash merges discard commit body footers.

    **Section 2: Module Scaffold Pattern**
    The six required files every module must contain:
    - `main.tf` — resource definitions
    - `variables.tf` — input variables
    - `outputs.tf` — output values (required even if minimal; needed for terraform-docs)
    - `versions.tf` — terraform {} block with required_version and provider requirements (NOT terraform.tf)
    - `README.md` — module documentation (terraform-docs inject markers required)
    - `tests/` — test directory (unit.tftest.hcl and tests/example/ added in Phase 5)

    Target directory pattern: `modules/{provider}/{resource}/` (e.g., `modules/docker/container/`)

    **Section 3: Autonomy Matrix**
    Use the locked table exactly:
    | Operation | Agent Autonomy |
    |-----------|----------------|
    | Edit/create .tf, .md, .yml, .json files | Freely — no approval needed |
    | Edit CI workflows (lint.yaml, release.yaml, etc.) | Freely — no approval needed |
    | Delete files | Must ask human first |
    | Force push | Must ask human first |
    | Create releases | Must ask human first |
    | Modify branch protection rules | Must ask human first |

    **Section 4: Consumer Source URL Pattern**
    Show the correct pattern and explicitly call out depth=1 as wrong:
    ```hcl
    # CORRECT — version-pinned ref, no depth=1
    module "container" {
      source = "github.com/Schillman/terraform-registry//modules/docker/container?ref=modules/docker/container/v1.0.0"
    }
    ```
    Include a pitfall note: `depth=1` must NOT be added to version-pinned source URLs. Once new commits land after a release tag, `depth=1` with an older pinned tag fails. Never use `?depth=1&ref=...`.

    CRITICAL: The string `depth=1` must not appear as a recommended example anywhere in the file. It may appear only as a documented pitfall to avoid.

    After writing, the file must be at least 40 lines.
  </action>
  <verify>
    <automated>test -f SKILL.md && grep -c "feat:" SKILL.md | grep -qv "^0$" && grep "patch bump" SKILL.md && grep "depth=1" SKILL.md && grep "modules/docker/container" SKILL.md && wc -l SKILL.md | awk '{if ($1 >= 40) print "OK: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>
    - SKILL.md exists at repo root with all four sections
    - Contains "patch bump" for docs:/chore:/refactor:/test:/ci: (not "no release")
    - Contains `depth=1` only as a pitfall warning, not as a recommended example
    - Contains `ref=modules/docker/container/v1.0.0` format in the consumer URL example
    - Contains the autonomy matrix with file-type permissions
    - At least 40 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLAUDE.md and .markdownlintignore</name>
  <files>CLAUDE.md, .markdownlintignore</files>
  <action>
    **Create CLAUDE.md:**
    Write a short file (10-20 lines) at the repo root. It must:
    - Tell Claude Code agents to read SKILL.md before doing any work
    - Reference SKILL.md by name so Claude Code's @ auto-loading can pick it up
    - NOT duplicate SKILL.md content inline — it is a pointer, not a mirror
    - Briefly state what SKILL.md contains (commit types, module scaffold, autonomy matrix, consumer URL format)

    Example structure (wording at discretion):
    ```markdown
    # Claude Code Agent Instructions

    Before doing any work in this repository, read @SKILL.md.

    SKILL.md contains all operating conventions for this repo:
    - Commit type → semver impact mapping (feat:, fix:, docs:, etc.)
    - Required files for every module (module scaffold pattern)
    - Autonomy matrix: what you can do freely vs. must ask the human first
    - Correct consumer source URL format (and why depth=1 is forbidden on version-pinned refs)
    ```

    **Create .markdownlintignore:**
    Write a single-line file at the repo root:
    ```
    .planning/
    ```
    This excludes the internal planning documentation from markdownlint. The `.planning/` directory uses informal formatting that does not conform to markdownlint rules.

    After both files are written, stage and commit all three new files (SKILL.md, CLAUDE.md, .markdownlintignore):
    ```
    git add SKILL.md CLAUDE.md .markdownlintignore
    git commit -m "docs: add SKILL.md, CLAUDE.md, and .markdownlintignore for agent conventions"
    ```
  </action>
  <verify>
    <automated>test -f CLAUDE.md && grep -i "SKILL.md" CLAUDE.md && test -f .markdownlintignore && grep "\.planning/" .markdownlintignore && echo "Both files verified"</automated>
  </verify>
  <done>
    - CLAUDE.md exists at repo root and contains a reference to SKILL.md
    - CLAUDE.md does NOT contain the full commit convention table or autonomy matrix inline (it is a pointer, not a mirror)
    - .markdownlintignore exists at repo root and contains .planning/
    - All three agent convention files (SKILL.md, CLAUDE.md, .markdownlintignore) are committed to git
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `test -f SKILL.md && echo "EXISTS"` outputs "EXISTS"
2. `grep "patch bump" SKILL.md` returns at least one match (confirms user-locked decision used)
3. `grep -c "depth=1" SKILL.md` returns at least 1 (it's documented as a pitfall) — confirm it appears only in a warning/pitfall context, NOT as a recommended example
4. `grep "modules/docker/container/v1.0.0" SKILL.md` returns a match (correct ref format)
5. `test -f CLAUDE.md && grep "SKILL.md" CLAUDE.md && echo "CLAUDE.md OK"` outputs "CLAUDE.md OK"
6. `wc -l CLAUDE.md | awk '{print $1}'` returns a number less than 30 (it's a thin pointer, not a full copy)
7. `test -f .markdownlintignore && grep "\.planning/" .markdownlintignore && echo "IGNORE OK"` outputs "IGNORE OK"
8. `git log --oneline -3` shows commits for both the module migration and agent conventions
</verification>

<success_criteria>
- SKILL.md exists with all four locked sections: commit convention table (patch bump for docs:/chore:), module scaffold, autonomy matrix, consumer URL without depth=1 (AGNT-01, AGNT-03, AGNT-04, AGNT-05)
- CLAUDE.md exists as a thin pointer to SKILL.md without duplicating content (AGNT-02)
- .markdownlintignore exists excluding .planning/ (MAINT-02)
- All files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` using the summary template.
</output>
