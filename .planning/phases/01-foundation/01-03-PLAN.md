---
phase: 01-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
autonomous: true
gap_closure: true
requirements:
  - STRC-01
  - STRC-02
  - STRC-03
  - STRC-04
  - STRC-05
  - STRC-06
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-05
  - MAINT-02

must_haves:
  truths:
    - "SKILL.md autonomy matrix contains a row stating feat:/fix: commits may be pushed freely without human approval"
    - "SKILL.md autonomy matrix contains a row stating feat!:/fix!: (breaking) changes require a human to verify tfbreak output before merging"
    - "tfbreak is named in SKILL.md as the tool for detecting breaking changes"
  artifacts:
    - path: "SKILL.md"
      provides: "Complete autonomy matrix with commit-type rules and tfbreak reference"
      contains: "tfbreak"
  key_links:
    - from: "SKILL.md autonomy matrix"
      to: "AGNT-04 requirement"
      via: "explicit commit-type rows in Section 3"
      pattern: "feat:|fix:.*Freely|tfbreak"
---

<objective>
Close AGNT-04 gap: add commit-type autonomy rows and a tfbreak reference to the SKILL.md autonomy matrix.

Purpose: The verification checker found SKILL.md's Section 3 (Autonomy Matrix) covers file-operation permissions but omits the commit-type rules mandated by AGNT-04 — specifically that feat:/fix: commits are fully autonomous and that feat!:/fix!: (breaking) changes require a human to verify tfbreak output before merging. tfbreak is not mentioned anywhere in SKILL.md.

Output: SKILL.md with two new rows in Section 3 and a brief tfbreak note.
</objective>

<execution_context>
@/Users/p950cvo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/p950cvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add commit-type autonomy rows and tfbreak reference to SKILL.md Section 3</name>
  <files>SKILL.md</files>
  <action>
    Read SKILL.md in full. Locate Section 3 "Autonomy Matrix". The table currently has six rows covering file-operation and operational permissions. Append two new rows to the bottom of that table:

    | Commit `feat:` or `fix:` changes | Freely — no human approval needed |
    | Commit `feat!:` or `fix!:` (breaking) | Requires human to verify tfbreak output before merging |

    Immediately after the table (before the `---` separator), add a single sentence note:

    > **tfbreak** is the tool used to detect breaking Terraform configuration changes. Run it in CI before merging any PR that carries a `feat!:` or `fix!:` commit.

    Do NOT change any other section. Do NOT reformat existing rows. Do NOT alter Section 1, 2, or 4. The final file must still pass `terraform fmt` (not applicable — this is Markdown, not HCL). Run `markdownlint SKILL.md` if available, otherwise verify manually that no blank lines were accidentally inserted inside the table.

    Commit message: `docs(01-03): add commit-type autonomy rows and tfbreak reference to SKILL.md`
  </action>
  <verify>
    <automated>grep -n "tfbreak" /Users/p950cvo/Files/p-repositories/terraform-registry/SKILL.md</automated>
  </verify>
  <done>
    - `grep "tfbreak" SKILL.md` returns at least one match
    - `grep "feat:" SKILL.md` returns a row in the autonomy matrix (not just Section 1 commit table)
    - `grep "fix!:" SKILL.md` returns a row in the autonomy matrix (not just Section 1 commit table)
    - The word "human" appears in association with the breaking-change row
    - No other sections modified
  </done>
</task>

</tasks>

<verification>
After execution:
1. `grep -n "tfbreak" SKILL.md` — must return at least 1 line
2. `grep -c "feat:" SKILL.md` — must be >= 2 (one in commit table, one in autonomy matrix)
3. `grep -c "fix!:" SKILL.md` — must be >= 2 (one in commit table, one in autonomy matrix)
4. `wc -l SKILL.md` — must be > 76 (was 76 before this change; two new rows + note = ~4 new lines)
5. Confirm Section 3 table still renders correctly (no broken Markdown pipe alignment)
</verification>

<success_criteria>
SKILL.md autonomy matrix explicitly covers:
- feat:/fix: = autonomous (no human needed)
- feat!:/fix!: = requires human tfbreak verification before merge
- tfbreak named as the breaking-change detection tool

AGNT-04 requirement fully satisfied. VERIFICATION.md re-run should score 12/12.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` using the standard summary template.
</output>
